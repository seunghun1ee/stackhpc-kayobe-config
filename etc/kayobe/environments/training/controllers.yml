---
controller_bootstrap_user: "{{ os_distribution if os_distribution == 'ubuntu' else 'rocky' }}"
# List of storage volume groups. See mrlesmithjr.manage-lvm role for
# format.
controller_lvm_groups:
  - "{{ stackhpc_lvm_group_rootvg }}"
